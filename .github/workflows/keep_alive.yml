name: Keep Supabase Alive

on:
  workflow_dispatch:

  schedule:
    - cron: "0 6 * * 1,4"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping database
        run: |
          curl -L https://siminvest-rouge.vercel.app/api/keepalive